---
version: '3'

tasks:
  default:
    desc: Deploy Grafana Operator
    cmds:
      - task: deploy

  repos:
    desc: Add Grafana Operator Helm repository
    vars:
      REPO_NAME: grafana
    cmds:
      - helm repo add {{.REPO_NAME}} https://grafana.github.io/helm-charts
      - helm repo update {{.REPO_NAME}}

  deploy:
    desc: Deploy Grafana Operator using Helm
    vars:
      MONITORING_NS: monitoring
      CHART_NAME: grafana-operator
      REPO_NAME: grafana
      RELEASE_NAME: grafana-operator
    cmds:
      - helm upgrade --install {{.RELEASE_NAME}} {{.REPO_NAME}}/{{.CHART_NAME}} -n {{.MONITORING_NS}} -f {{.TASKFILE_DIR}}/values.yaml

  clean:
    desc: Remove Grafana Operator
    vars:
      MONITORING_NS: monitoring
      RELEASE_NAME: grafana-operator
    cmds:
      - helm uninstall {{.RELEASE_NAME}} -n {{.MONITORING_NS}} --ignore-not-found
