apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
  namespace: security
spec:
  secretName: test-cert-tls
  issuerRef:
    name: cloudflare
    kind: ClusterIssuer
  commonName: test.biduleproofzone.ovh
  dnsNames:
    - test.biduleproofzone.ovh
